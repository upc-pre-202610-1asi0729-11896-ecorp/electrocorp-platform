{
  "name": "electrocorp-fake-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port ${PORT:-3000} db.json"
  },
  "dependencies": {
    "json-server": "0.17.4"
  }
}
